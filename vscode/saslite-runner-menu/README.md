# SASLite Runner Menu

Adds **SASLite: Run with Full Log** to the editor Run menu for `.sas` files.
The command delegates to Code Runner's configured `customCommand`; the normal
play action continues to use the concise fail-fast executor.
